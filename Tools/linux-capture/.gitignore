__pycache__/
*.pyc
.venv/
.venv-ble/
# Ignore ALL JSON in this tool dir — captures + any `export --out <name>.json` decode dumps
# (they carry HR and timeline; the .db also carries the strap serial / session token / MAC).
*.json
captures/
*.db
*.db-wal
*.db-shm
